# Live Praise v0.0.9 (legado arquivado)

Código-fonte histórico do Live Praise antes da refatoração (Electron 29, Express, Socket.IO, jQuery/Bootstrap).

**Status:** arquivo — sem evolução activa. Referência funcional para paridade com baseline (escopo sec. 8).

Para executar o legado:

```bash
cd v0.0.8
npm install
npm start
```

O código activo da refatoração vive na **raiz** do repositório.
