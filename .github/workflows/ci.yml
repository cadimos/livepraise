# CI leve: smokes em PR (sem gerar instaladores)
name: CI

on:
  pull_request:
    branches: [master, main]

jobs:
  typecheck:
    name: typecheck
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-node@v5
        with:
          node-version: '22'
          cache: npm

      - run: npm ci
      - run: npm run check:js-in-src
      - run: npm run lint
      - run: npm run typecheck

  smoke:
    name: smoke tests
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-node@v5
        with:
          node-version: '22'
          cache: npm

      - run: npm ci
      - run: npm run test:video-pipeline
      - run: npm run smoke:bootstrap
      - run: npm run smoke:fase8

  resolve-version-dry:
    name: validar incremento de versão
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - uses: actions/setup-node@v5
        with:
          node-version: '22'

      - name: Simular próxima versão (sem alterar ficheiros)
        run: npm run resolve:version:dry
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
